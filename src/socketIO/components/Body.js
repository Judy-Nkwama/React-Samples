import React from 'react';
import ChatRoom from './Components';
import ChatDetails from './ChatDetails';

const Body = props => {
    return(
        <div className=""> 
            body
        </div>
    );
};

export default Body;